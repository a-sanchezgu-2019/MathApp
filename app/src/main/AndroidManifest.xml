<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ejemplo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="MathApp"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ecuaciones2"></activity>
        <activity android:name=".ecuaciones" />
        <activity android:name=".Activity_ayuda" />
        <activity android:name=".configuracion" />
        <activity android:name=".calculadora" />
        <activity android:name=".menu" />
        <activity android:name=".conversion.ConversionActivity" />
        <activity android:name=".calculadora_trigonometrica" />
        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>